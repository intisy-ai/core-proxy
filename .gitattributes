# The emitted declarations and bundle are compared byte for byte against a fresh emission, and both
# the processor and TeaVM write LF. Without this, core.autocrlf checks them out as CRLF and the
# drift gate fails on a fresh clone.
src/generated/* text eol=lf

# A CRLF gradlew is not executable on Linux.
gradlew text eol=lf
