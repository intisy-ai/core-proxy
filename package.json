{
  "name": "core-proxy",
  "version": "1.2.0",
  "description": "Generic routing and rate-limit proxy engine for the intisy-ai AI-proxy ecosystem.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "author": "intisy",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/intisy-ai/core-proxy.git"
  },
  "keywords": [
    "proxy",
    "routing",
    "anthropic",
    "ai",
    "intisy",
    "core-proxy"
  ],
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm run build:teavm && tsc && node node_modules/@intisy-ai/api/scripts/copy-generated.mjs",
    "build:teavm": "node node_modules/@intisy-ai/api/scripts/teavm-build.mjs --java-dir . --module :teavm --out src/generated/core-proxy.teavm.js",
    "test": "vitest run",
    "prepublishOnly": "npm run build",
    "emit": "node node_modules/@intisy-ai/api/scripts/emit-dts.mjs --java-dir . --module :proxy-contracts --module-dir proxy-contracts --out src/generated"
  },
  "dependencies": {
    "@intisy-ai/api": "^1.1.0",
    "@intisy-ai/core-ir": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
