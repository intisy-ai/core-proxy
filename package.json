{
  "name": "core-proxy",
  "version": "0.1.0",
  "description": "Shared inbound proxy daemon + tier routing for the intisy loaders and dashboard",
  "type": "module",
  "main": "dist/index.js",
  "license": "MIT",
  "author": "intisy",
  "repository": { "type": "git", "url": "git+https://github.com/intisy-ai/core-proxy.git" },
  "keywords": ["proxy", "routing", "anthropic", "ai", "intisy", "core-proxy"],
  "files": ["dist/", "README.md", "LICENSE"],
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "build": "npm --prefix core-ir install && npm --prefix core-ir run build && tsc",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": { "typescript": "^5.6.0", "vitest": "^2.1.0", "@types/node": "^22.0.0", "esbuild": "^0.21.5" }
}
